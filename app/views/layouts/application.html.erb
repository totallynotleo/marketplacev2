<!DOCTYPE html>
<html>
  <head>
    <title>Marketplace</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
    <div class="page-header">
      <h1> <%= link_to "EventSpark", root_path %></h1>
    </div>


    <div class="navbar">
      <%= link_to "My Account", current_user, method: :get, :class => 'navbar-link' %>
      <% if current_user == nil %>
      <% elsif current_user.admin == true %>
        <a href="/users", class="navbar-link"> Users </a>  
      <% end %>
      <a href="/listings", class="navbar-link"> Listings </a>
      
      <%= link_to cart_path(@cart) do %> 
        Cart
      <% end %>
    </div>
  </header>

  <div class="devise-stuff">    
    <p>
      <% if user_signed_in? %>
        Logged in as <strong><%= current_user.email %></strong>.
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
      <% else %>
        <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
      <% end %>
      <br>
      <%= alert %>
      </p>
  </div>
  <div class="main-program">
    <%= yield %>
  </div>

  <footer>
    <p>
      | <%= link_to "Home", root_path %> |
      | Privacy policy |
      | Contact |
    </p>
  </footer>
  
  </body>
</html>
